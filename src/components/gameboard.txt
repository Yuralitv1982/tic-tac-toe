Создаём массив initialGameBoard

Это наше стартовое игровое поле (матрица 3x3), где все клетки пустые (null).

Используем useState для управления игровым полем

gameBoard — текущее состояние поля.

setGameBoard — функция обновления состояния.

Функция updateGameBoard() обновляет поле на основе ходов

Копируем initialGameBoard, чтобы не менять оригинальный массив.

Заполняем клетки данными из turns (массива ходов).

Обновляем gameBoard через setGameBoard(updatedBoard).

Автоматическое обновление игрового поля

При изменении turns, вызывается updateGameBoard() для обновления поля.

Рендерим игровое поле и кнопки для каждого квадрата

Если playerSymbol === null, то клетка пустая.

При клике вызывается onSelectSquare(rowIndex, colIndex), чтобы записать ход.